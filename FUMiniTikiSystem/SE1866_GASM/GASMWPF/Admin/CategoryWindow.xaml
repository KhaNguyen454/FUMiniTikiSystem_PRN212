<Window x:Class="GASMWPF.Admin.CategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GASMWPF.Admin"
        mc:Ignorable="d"
        Title="Quản lý danh mục" 
        Height="600" 
        Width="900"
        Background="#EAF1FB"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#888888" 
                                                ShadowDepth="2" 
                                                BlurRadius="5" 
                                                Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2C3E50"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#3498DB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Default image for categories without pictures -->
        <DrawingImage x:Key="DefaultCategoryImage">
            <DrawingImage.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#F0F0F0">
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="0,0,60,60" />
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="#AAAAAA">
                        <GeometryDrawing.Geometry>
                            <PathGeometry Figures="M30,15 C25,15 21,19 21,24 C21,29 25,33 30,33 C35,33 39,29 39,24 C39,19 35,15 30,15 Z M45,45 L15,45 L15,42 C15,36 25,33 30,33 C35,33 45,36 45,42 L45,45 Z" />
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingImage.Drawing>
        </DrawingImage>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- DataGrid hiển thị danh mục -->
        <Border Grid.Row="0" 
                Background="White" 
                CornerRadius="8" 
                BorderBrush="#D0D0D0" 
                BorderThickness="1"
                Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect Color="#888888" 
                                ShadowDepth="2" 
                                BlurRadius="5" 
                                Opacity="0.3"/>
            </Border.Effect>
            <DataGrid x:Name="CategoryGrid"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Margin="0"
                      RowHeight="70"
                      FontSize="14"
                      FontFamily="Segoe UI"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      Background="Transparent"
                      BorderThickness="0"
                      AlternatingRowBackground="#F8FAFC"
                      RowBackground="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" 
                                      Binding="{Binding CategoryId}" 
                                      Width="80" 
                                      FontWeight="Medium"/>
                    <DataGridTemplateColumn Header="Hình ảnh" 
                                          Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="5" HorizontalAlignment="Center">
                                    <Image Width="60" 
                                           Height="60" 
                                           Stretch="Uniform">
                                        <Image.Effect>
                                            <DropShadowEffect BlurRadius="3" 
                                                              ShadowDepth="1" 
                                                              Opacity="0.2"/>
                                        </Image.Effect>
                                        <Image.Source>
                                            <PriorityBinding>
                                                <Binding Path="Picture" FallbackValue="{StaticResource DefaultCategoryImage}" TargetNullValue="{StaticResource DefaultCategoryImage}"/>
                                                <Binding Source="{StaticResource DefaultCategoryImage}"/>
                                            </PriorityBinding>
                                        </Image.Source>
                                    </Image>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Tên danh mục" 
                                      Binding="{Binding Name}" 
                                      Width="220" 
                                      FontWeight="Medium"/>
                    <DataGridTextColumn Header="Mô tả" 
                                      Binding="{Binding Description}" 
                                      Width="*"
                                      FontWeight="Regular"/>
                </DataGrid.Columns>
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#3498DB"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        <Setter Property="BorderBrush" Value="#2980B9"/>
                    </Style>
                </DataGrid.Resources>
            </DataGrid>
        </Border>

        <!-- Form thêm/sửa -->
        <Border Grid.Row="1" 
                Background="White" 
                Padding="25" 
                CornerRadius="8" 
                BorderBrush="#D0D0D0" 
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#888888" 
                                ShadowDepth="2" 
                                BlurRadius="5" 
                                Opacity="0.3"/>
            </Border.Effect>
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Thông tin danh mục" 
                          FontSize="18" 
                          FontWeight="Bold" 
                          Margin="0,0,0,15" 
                          Foreground="#2C3E50"
                          FontFamily="Segoe UI"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" 
                              Text="Tên:" 
                              VerticalAlignment="Center" 
                              FontSize="14" 
                              FontWeight="Medium"/>
                    <TextBox Grid.Row="0" Grid.Column="1" 
                            x:Name="txtName" 
                            Style="{StaticResource ModernTextBoxStyle}" 
                            Margin="0,5,0,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" 
                              Text="Mô tả:" 
                              VerticalAlignment="Center" 
                              FontSize="14" 
                              FontWeight="Medium"/>
                    <TextBox Grid.Row="1" Grid.Column="1" 
                            x:Name="txtDescription" 
                            Style="{StaticResource ModernTextBoxStyle}" 
                            Margin="0,5,0,10"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" 
                              Text="Hình ảnh:" 
                              VerticalAlignment="Center" 
                              FontSize="14" 
                              FontWeight="Medium"/>
                    <Grid Grid.Row="2" Grid.Column="1" Margin="0,5,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                x:Name="txtPicture" 
                                Style="{StaticResource ModernTextBoxStyle}"
                                IsReadOnly="True"/>
                        <Button Grid.Column="1"
                                Content="Chọn ảnh" 
                                Click="BtnBrowseImage_Click"
                                Width="100" 
                                Height="36" 
                                Style="{StaticResource ModernButtonStyle}"
                                Margin="10,0,0,0"/>
                    </Grid>

                    <!-- Buttons -->
                    <StackPanel Grid.Row="3" Grid.Column="1" 
                               Orientation="Horizontal" 
                               HorizontalAlignment="Right" 
                               Margin="0,20,0,0">
                        <Button Content="Thêm" 
                                Click="BtnAdd_Click" 
                                Width="100" 
                                Height="40" 
                                Style="{StaticResource ModernButtonStyle}" 
                                Background="#27AE60" 
                                Margin="0,0,10,0"/>
                        <Button Content="Cập nhật" 
                                Click="BtnUpdate_Click" 
                                Width="100" 
                                Height="40" 
                                Style="{StaticResource ModernButtonStyle}" 
                                Background="#3498DB" 
                                Margin="0,0,10,0"/>
                        <Button Content="Xóa" 
                                Click="BtnDelete_Click" 
                                Width="100" 
                                Height="40" 
                                Style="{StaticResource ModernButtonStyle}" 
                                Background="#E74C3C" 
                                Margin="0"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>